import styled from 'styled-components'

const Container = styled.div`

	color: ${({ theme }) => theme.colors.dullTxt};
	background-color: ${({ theme }) => theme.colors.lighterBg};
`
export default Container
